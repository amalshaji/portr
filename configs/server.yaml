admin:
  port: 8000
  oauth:
    clientId: $GITHUB_CLIENT_ID # this is replaced by the environment var
    clientSecret: $GITHUB_CLIENT_SECRET
ssh:
  port: 2222
proxy:
  port: 8001
database:
  url: $DATABASE_URL
  autoMigrate: true
useLocalhost: true
debug: true
domain: $DOMAIN
