---
title: HTTP Tunnel
description: Learn how to start a http tunnel using portr
---

import { Callout } from 'fumadocs-ui/components/callout';

Use the following command to tunnel an HTTP connection.

```bash
portr http 9000
```

Or start it with a custom subdomain:

```bash
portr http 9000 -s amal-test
# or
portr http 9000 --subdomain amal-test
```

<Callout type="info">
  This also starts the portr inspector on [http://localhost:7777](http://localhost:7777) by default, where you can inspect and replay HTTP requests. Change `dashboard_port` in the client config to move it to another local port, or set `disable_dashboard: true` to skip starting it entirely.
</Callout>

## WebSocket Worker Pool

HTTP tunnels use WebSocket connections from the client to the server. By default,
each HTTP tunnel starts two workers so concurrent incoming requests can keep
moving while another request is in flight. Configure `pool_size` on a tunnel
template when you want a different worker count:

```yaml
tunnels:
  - name: api
    subdomain: api-dev
    port: 3000
    type: http
    pool_size: 4
```

TCP tunnels use a single worker because the server assigns a remote TCP port for
that tunnel connection.

## How it works

When you run the HTTP tunnel command:

1. Portr connects to your configured server
2. Opens WebSocket tunnel workers to the configured `ws_url`
3. Starts the request inspector interface locally
4. Routes all incoming requests through the tunnel to your local service

## Inspector Features

The Portr inspector provides powerful debugging capabilities:

- **Request inspection**: View all incoming HTTP requests in real-time
- **Request replay**: Replay a stored request as-is or edit method, path, headers, and body before resending
- **Headers analysis**: Examine request and response headers
- **Payload viewing**: Inspect request and response bodies
- **WebSocket inspection**: Monitor upgraded WebSocket sessions and captured frames

## Common Use Cases

- **Local web development**: Share your local development server with others
- **Webhook testing**: Receive webhooks from external services during development
- **API debugging**: Test API integrations with external services
- **Demo presentations**: Show your work to clients or team members
